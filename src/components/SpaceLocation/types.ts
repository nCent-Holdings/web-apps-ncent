import { WellcubeLocation } from '../../api-types/wellcube';

export type SpaceLocationProps = {
  location?: WellcubeLocation;
  inline?: boolean;
  classExtend?: string;
  localClassExtend?: string;
  topClassExtend?: string;
};
